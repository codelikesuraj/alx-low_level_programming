#include <stdio.h>
#include <stdlib.h>

/**
 * main - no description (testing this)
 * Return: Always (0) Success
 */
int main(void)
{
	int numbers = 48;

	while (numbers < 58)
	{
		putchar(numbers);
		numbers++;
	}
	putchar('\n');

	return (0);
}
