#include "main.h"

/**
 * print_alphabet - prints the alphabets
 * in lower case
 *
 * Return: 0
 */
void print_alphabet(void)
{
	char alphabets = [];
	
	for (int i = a; a <= z; a++)
		_putchar(i);
	_putchar('\n');
}
